/// <reference types="node" />
import * as nbin from "@coder/nbin";
export declare class Binary implements nbin.Binary {
    private readonly options;
    private readonly fs;
    constructor(options: nbin.BinaryOptions);
    writeFile(pathName: string, content: Buffer | string): void;
    writeFiles(globName: string, callback?: (fileWritten: string) => void): number;
    writeModule(modulePath: string): void;
    build(): Promise<Buffer>;
    private cacheBinary;
    private fetchNodeBinary;
    private get nodeBinaryName();
    private get version();
}
